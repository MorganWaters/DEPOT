package eDepotSystem;

import java.util.Scanner;

public class Vehicle {
	public static String make;
	public static String model;
	public static int weight;
	public static String regNo;
	
	public Vehicle(String make, String model, Int weight, Boolean isAvailable) {
		this.make = make;
		this.model = model;
		this.weight = weight;
		this.regNo = regNo;
    
  private class Tanker {
    private static int liquidCapacity;
    private static String liquidType
    
  private class Truck
    private static int cargoCapacity;
		
    setSchedule(schedule);
    setIsAvailable(isAvailable);
    
    vehicle = new boolean[tanker][truck]
     for(int i = tanker  i>0,  i--){
        type[i] = true;
	   for(int x = truck; x>0; x--){
        !type[i] = true;
    }
  }
	
	public static String getMake() {
		return make;
	}
  
  public static String getModel() {
		return model;
	}

	public static int weight() {
		return weight;
	}

	public static String getRegNo() {
		return regNo;
	}

  public boolean isAvailable(int vehicle, int Schedule) {
    return !seat[vehicle][Schedule];
    
    for (int vehicle = 0; vehicle < getVehicle(); vehicle++) {
    for (int Schedule = 0; schedule < getSchedule(); schedule++) {
         if (isAvailable(vehicle, schedule)) {
             System.out.println("The vehicle is available");
         }
     } 
 }
